name: Build and Release

on:
  push:
    branches: [ main ]
    tags: ['v*']
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libdbus-1-dev

      - name: Build C driver (host, verify compilation)
        run: |
          gcc -Os -std=c99 -Wall -D_GNU_SOURCE \
            $(pkg-config --cflags dbus-1) \
            -o victron-bms src/victron-bms.c \
            -lm $(pkg-config --libs dbus-1)
          file victron-bms

      - name: Make scripts executable
        run: chmod +x scripts/*.sh

      - name: Create package
        run: |
          VERSION="$(cat VERSION)"
          OUTPUT_FILE="superb-victron-integration-v$VERSION.tar.gz"
          tar --exclude=.git --exclude=.gitignore --exclude='*.tar.gz' \
              -czf "/tmp/$OUTPUT_FILE" .
          mv "/tmp/$OUTPUT_FILE" .
          echo "Created $OUTPUT_FILE"
          echo "Created $OUTPUT_FILE"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: victron-package
          path: "*.tar.gz"

      - name: Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: "*.tar.gz"
